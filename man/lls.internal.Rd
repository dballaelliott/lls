% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lls.R
\name{lls.internal}
\alias{lls.internal}
\title{Internal LLS function that handles estimation.}
\usage{
lls.internal(
  dat,
  y = NULL,
  x = NULL,
  dy = NULL,
  dx = NULL,
  r = NULL,
  weights = NULL,
  FE.fml = NULL,
  control.fml = NULL,
  pointmass.zero = FALSE,
  trim.zero = NULL,
  bandwidth = NULL,
  normalize.x = FALSE,
  normalize.r = TRUE,
  r.support.points = nrow(dat),
  bootstrap = FALSE,
  bootstrap.bayesian = TRUE,
  bootstrap.n = 200,
  ci.level = 0.95,
  cluster = NULL,
  n.cores = availableCores(omit = 2),
  kernel = epanechnikov,
  mode = "iv",
  auto.trim = TRUE,
  sd.trim = 0.02,
  user.call.level = TRUE
)
}
\arguments{
\item{dat}{A data frame or data.table containing the analysis data}

\item{y}{Character string specifying the outcome variable name in `dat` (for IV mode)}

\item{x}{Character string specifying the treatment variable name in `dat` (for IV mode)}

\item{dy}{Character string specifying the outcome variable name in `dat` (for panel mode)}

\item{dx}{Character string specifying the treatment variable name in `dat` (for panel mode)}

\item{r}{Optional; name of variable in `dat` giving ranks (IV mode) or vector of ranks directly if pre-computed (default: NULL)}

\item{weights}{Optional; character string specifying the weights variable name in `dat` (default: NULL)}

\item{FE.fml}{Optional; formula string for fixed effects specification (default: NULL)}

\item{control.fml}{Optional; formula string for control variables (default: NULL)}

\item{pointmass.zero}{Logical; if TRUE, will use near(x,0) instead of kernel(x,0) (default: FALSE)}

\item{trim.zero}{Numeric; threshold for trimming support points away from near zero values (default: NULL, uses bandwidth/2)}

\item{bandwidth}{Numeric; bandwidth value, if NULL will use rule-of-thumb (default: NULL)}

\item{normalize.x}{Logical; whether to use ranks of x instead of raw values (default: FALSE; panel mode only)}

\item{normalize.r}{Logical; whether to normalize the running variable r to be equally spaced between 0 and 1 within sign groups (default: TRUE)}

\item{r.support.points}{Integer; number of support points to use in estimation (default: nrow(dat))}

\item{bootstrap}{Logical; whether to perform bootstrap inference (default: FALSE)}

\item{bootstrap.bayesian}{Logical; whether to use Bayesian bootstrap (TRUE) or standard bootstrap (default: TRUE)}

\item{bootstrap.n}{Integer; number of bootstrap iterations (default: 200)}

\item{ci.level}{Numeric; confidence level for intervals between 0 and 1 (default: 0.95)}

\item{cluster}{Optional; character string specifying cluster variable for clustered standard errors (default: NULL)}

\item{n.cores}{Integer; number of cores to use for parallel processing (default: availableCores(omit=2))}

\item{kernel}{Function; kernel function to use (default: epanechnikov)}

\item{mode}{Character string specifying estimation mode: "iv" (default) or "panel"}

\item{auto.trim}{Logical; if TRUE will trim the support points away from zero to avoid truncation. Sets trim.zero. (default: TRUE)}

\item{sd.trim}{Numeric; trimming proportion for SD calculation in bootstrap (default: 0.02, 0 or FALSE disables trimming)}
}
\description{
Don't call this -- use the exported `lls(), iv.lls(), panel.lls()` functions instead.
}
\keyword{internal}
